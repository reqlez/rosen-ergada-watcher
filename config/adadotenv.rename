# Required env vars for ADA Watcher and ADA DB. Rename to: adadotenv
# Ports can be the same for adadb and ergdb as they are not used outside of rosen_network
POSTGRES_PASSWORD=
POSTGRES_USER=
POSTGRES_DB=
POSTGRES_PORT=5432
